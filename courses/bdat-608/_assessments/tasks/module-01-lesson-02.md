# Task: Module 1, Lesson 2 — Environment Verification

## Objective

Confirm your modelling environment is fully set up and produce a reproducible Rmd document.

## Instructions

1. Open your `exercise.Rmd` and ensure all tasks are complete (all packages load, package check prints all `TRUE`, sim1 plot renders).

2. Knit the document and verify:
   - No red error messages appear
   - The scatter plot of `sim1` is visible in the output
   - The package check shows all `TRUE`

3. Answer this question in a comment in your Rmd:

   > The `broom` package provides three functions: `tidy()`, `augment()`, and `glance()`. Without looking them up yet, predict what each one might return based on its name. (We will verify your predictions in later modules.)

## Submission

Submit the knitted HTML file. The scatter plot and package check must be visible.
