---
title: "BDAT 608 | Module 3, Lesson 2 — Diagnostic Plots and Model Comparison"
author: "Your Name"
date: "`r Sys.Date()`"
output:
  html_document:
    toc: true
    theme: flatly
    highlight: tango
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE, message = FALSE, warning = FALSE,
                      fig.width = 8, fig.height = 5)
```

```{r libraries}
library(tidyverse)
library(modelr)
library(ggfortify)
options(na.action = na.warn)
data("sim1", package = "modelr")
sim1_mod <- lm(y ~ x, data = sim1)
```

---

## Task 1 — Base R Diagnostics

```{r base-diagnostics, fig.width=9, fig.height=7}
# TODO: Produce the 2×2 base R diagnostic panel for sim1_mod.
# Use par(mfrow = c(2, 2)) before and par(mfrow = c(1, 1)) after.
```

---

## Task 2 — ggfortify Diagnostics

```{r ggplot-diagnostics}
# TODO: Use autoplot() to produce the same four diagnostics.
# Set colour = "#1B3A6B" and smooth.colour = "#C9A84C".
```

---

## Task 3 — Fit Quadratic Model

```{r quadratic-model}
# TODO: Fit mod_b <- lm(y ~ x + I(x^2), data = sim1)
# Print summary(mod_b)
```

---

## Task 4 — Compare Fitted Lines

```{r compare-fits}
mod_a <- sim1_mod  # linear (already fitted)

# TODO: Use gather_predictions(mod_a, mod_b) to overlay both models on sim1.
# Facet by model.
```

---

## Task 5 — Compare Residuals

```{r compare-residuals}
# TODO: Use gather_residuals(mod_a, mod_b) to compare residuals.
# Plot x vs resid, faceted by model. Add geom_ref_line(h = 0).
```

---

## Task 6 — AIC Comparison

```{r aic}
# TODO: Run AIC(mod_a, mod_b). Which model is preferred?
# Write your interpretation as a comment.
```

---

## Reflection

<!-- Is non-normal residuals always a sign that the model is wrong?
     When does normality matter and when does it not? (3–4 sentences) -->
