---
title: "BDAT 608 | Module 3, Lesson 1 — Predictions and Residuals with modelr"
author: "Your Name"
date: "`r Sys.Date()`"
output:
  html_document:
    toc: true
    theme: flatly
    highlight: tango
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE, message = FALSE, warning = FALSE)
```

```{r libraries}
library(tidyverse)
library(modelr)
options(na.action = na.warn)
data("sim1", package = "modelr")
sim1_mod <- lm(y ~ x, data = sim1)
```

---

## Task 1 — Prediction Grid

```{r prediction-grid}
# TODO: Use data_grid(x) to build a prediction grid,
# then add predictions from sim1_mod.
grid <- sim1 |>
  data_grid(x) |>
  add_predictions(# TODO: model name)

print(grid)
```

---

## Task 2 — Plot Data and Fitted Line

```{r fitted-line-plot}
# TODO: Create a plot with:
# - geom_point for the observed sim1 data (colour = "#1B3A6B")
# - geom_line for the fitted predictions from `grid` (colour = "#C9A84C")
# - appropriate axis label: y = expression(y ~ "/" ~ hat(y))
```

---

## Task 3 — Residual Distribution

```{r residual-distribution}
# TODO: Add residuals to sim1 using add_residuals(sim1_mod).
# Then plot a frequency polygon (geom_freqpoly, binwidth = 0.5) of the residuals.
# Add a dashed vertical line at x = 0.
sim1 <- sim1 |> add_residuals(# TODO)
```

---

## Task 4 — Residuals vs x

```{r residuals-vs-x}
# TODO: Plot residuals vs x.
# Use geom_ref_line(h = 0) to add a horizontal reference at zero.
# Is there a visible pattern? Write your answer as a comment.
```

---

## Reflection

<!-- What would a curved band in the residuals-vs-x plot mean? What would you do? -->
