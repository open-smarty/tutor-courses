---
title: "Exercise: Predictions and Residuals with modelr"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE, message = FALSE, warning = FALSE)
```

```{r load-packages}
library(tidyverse)
library(modelr)
```

```{r fit-model}
data("diamonds", package = "ggplot2")
mod_loglog <- lm(log(price) ~ log(carat), data = diamonds)
```

## Task 1: Create a prediction grid and overlay on scatter

```{r task1}
# TODO: Create a data_grid() with 200 evenly spaced carat values.
# Use add_predictions(mod_loglog, var = "log_pred") to attach log(price) predictions.
# Add a column pred = exp(log_pred) to back-transform to the price scale.
# Plot: scatter of price vs carat (alpha = 0.05) + geom_line() of pred vs carat.

```

## Task 2: Attach residuals and plot vs log(carat)

```{r task2}
# TODO: Use add_residuals(mod_loglog, var = "resid") on the diamonds data.
# Store as diamonds_r.
# Plot resid (y) vs log(carat) (x). Add a horizontal reference line at 0.

```

## Task 3: Colour residual plot by cut

```{r task3}
# TODO: Repeat the plot from Task 2 but add colour = cut to aes().
# Describe in a comment: do residuals cluster by cut?
# Which cut has the most negative residuals?

```

## Task 4: Mean residual per cut level

```{r task4}
# TODO: Use group_by(cut) |> summarise(mean_resid = mean(resid))
# on diamonds_r. Arrange in descending order.
# Which cut has the highest mean residual? Write an interpretation.

```
