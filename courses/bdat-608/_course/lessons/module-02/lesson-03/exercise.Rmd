---
title: "Exercise: Closed-Form OLS with lm()"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE, message = FALSE, warning = FALSE)
```

```{r load-packages}
library(tidyverse)
library(modelr)
library(broom)
```

## Task 1: Fit the linear model and decode summary()

```{r task1}
data("diamonds", package = "ggplot2")

# TODO: Fit lm(price ~ carat, data = diamonds). Store as mod_linear.
# Print summary(mod_linear).
# In comments below, record:
#   - intercept estimate
#   - slope estimate
#   - residual standard error
#   - R²
#   - F-statistic and its p-value

```

## Task 2: Interpret the slope

```{r task2}
# TODO: Extract the slope coefficient with coef(mod_linear)["carat"].
# Write a plain-English interpretation in a comment.
# e.g. "A 1-carat increase in diamond weight is associated with a $___
#       increase in price, on average, holding nothing else constant."

```

## Task 3: Fit the log-log model

```{r task3}
# TODO: Fit lm(log(price) ~ log(carat), data = diamonds).
# Store as mod_loglog. Print summary().
# Record the slope and R².

```

## Task 4: Interpret the log-log slope (price elasticity)

```{r task4}
# TODO: Extract the slope from mod_loglog.
# Write the interpretation: "A 1% increase in carat weight is associated
# with a ___% increase in price."
# This is the price elasticity of carat.

```

## Task 5: Compare models

```{r task5}
# TODO: Print R² for both models side by side.
# Write 2 sentences explaining why the log-log model fits better.

```

## Task 6: Tidy the log-log model with broom

```{r task6}
# TODO: Use broom::tidy(mod_loglog) to get a clean coefficient table.
# Use broom::glance(mod_loglog) to get model-level statistics.
# Identify the coefficient with the largest t-value and explain what that means.

```
