---
title: "Exercise: Search Strategies and Numerical Optimisation"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE, message = FALSE, warning = FALSE)
```

```{r load-packages}
library(tidyverse)
library(modelr)

data("sim1", package = "modelr")
```

## Task 1: RMSE function for sim1

```{r task1}
# TODO: Write rmse_sim1(b0, b1) that:
# 1. Computes pred <- b0 + b1 * sim1$x
# 2. Computes resid <- sim1$y - pred
# 3. Returns sqrt(mean(resid^2))
# Test it: rmse_sim1(0, 0) should give the RMSE of a flat-zero model.

```

## Task 2: Grid search

```{r task2}
# TODO: Create a grid:
#   b0 = seq(-5, 20, length = 25)
#   b1 = seq(1,  3,  length = 25)
# Use expand.grid() and then mutate() with map2_dbl to add rmse.
# Report the row with minimum rmse using slice_min().

```

## Task 3: Numerical optimisation with optim()

```{r task3}
# TODO: Write a wrapper rmse_vec(params) that extracts params[1] and params[2]
# as b0 and b1 and calls rmse_sim1().
# Then call optim(c(0, 0), rmse_vec, method = "BFGS").
# Print par, value, and convergence.

```

## Task 4: Compare to lm()

```{r task4}
# TODO: Fit lm(y ~ x, data = sim1).
# Print coef() side by side with the grid-search best and optim() result.
# Which is closest to lm()?

```

## Task 5: Visualise the grid search

```{r task5}
# TODO: Plot b0 (x) vs b1 (y) from the grid, coloured by rmse.
# Use scale_fill_viridis_c() or scale_colour_viridis_c().
# Optionally add a red point at the optim() solution.

```
