---
title: "BDAT 608 | Module 2, Lesson 2 — Search Strategies and Numerical Optimisation"
author: "Your Name"
date: "`r Sys.Date()`"
output:
  html_document:
    toc: true
    theme: flatly
    highlight: tango
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE, message = FALSE, warning = FALSE)
```

```{r libraries}
library(tidyverse)
library(modelr)
options(na.action = na.warn)
data("sim1", package = "modelr")

model1 <- function(a, data) a[1] + data$x * a[2]
measure_distance <- function(mod, data) sqrt(mean((data$y - model1(mod, data))^2))
sim1_dist <- function(a1, a2) measure_distance(c(a1, a2), sim1)
```

---

## Task 1 — Random Search (500 candidates)

```{r random-search}
set.seed(7)
# TODO: Generate 500 random models with a1 in [-20, 40] and a2 in [-5, 5].
# Compute dist for each and store in a tibble called `models`.
models <- tibble(
  a1 = runif(500, -20, 40),
  a2 = # TODO
) |>
  mutate(dist = # TODO
  )
```

```{r top10-plot}
# TODO: Plot sim1 scatter + top 10 lines (ranked by dist), coloured by -dist.
# Use scale_colour_viridis_c(guide = "none").
```

---

## Task 2 — RMSE Landscape

```{r rmse-landscape}
# TODO: Plot a1 vs a2 from `models`, coloured by dist.
# Use scale_colour_viridis_c(name = "RMSE").
# Add a title "RMSE landscape over (intercept, slope)".
```

---

## Task 3 — optim()

```{r optim-fit}
# TODO: Use optim() with par = c(0, 0) and fn = measure_distance to fit sim1.
# Print the intercept, slope, and minimum RMSE.
best <- optim(
  par  = c(0, 0),
  fn   = measure_distance,
  data = sim1
)
# print(best$par)
# print(best$value)
```

---

## Task 4 — Comparison

```{r comparison}
# TODO: Find the row in `models` with the minimum dist.
# How close is it to best$par? Print the comparison.
```

---

## Reflection

<!-- In 2–3 sentences: what is the risk of poor starting values for optim()?
     How would you mitigate it? -->
