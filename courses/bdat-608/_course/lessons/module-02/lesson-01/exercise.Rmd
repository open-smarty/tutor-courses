---
title: "Exercise: Loss Functions — RMSE and MAE"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE, message = FALSE, warning = FALSE)
```

```{r load-packages}
library(tidyverse)
library(modelr)
```

```{r sample-data}
set.seed(42)
d100 <- diamonds |> slice_sample(n = 100)
```

## Task 1: Implement RMSE and MAE

```{r task1}
# TODO: Write rmse_fn(b0, b1, data) that:
# 1. Computes predictions: pred <- b0 + b1 * data$carat
# 2. Computes residuals: resid <- data$price - pred
# 3. Returns sqrt(mean(resid^2))

# TODO: Write mae_fn(b0, b1, data) that returns mean(abs(resid))

```

## Task 2: Evaluate at a candidate parameter pair

```{r task2}
# TODO: Evaluate both functions at b0 = -2000, b1 = 7000, data = d100.
# Print both values. Which is larger? (RMSE is always >= MAE for the same data.)

```

## Task 3: Random search over 250 parameter pairs

```{r task3}
set.seed(7)
# TODO: Create a tibble with 250 rows:
#   b0 = runif(250, -5000, 0)
#   b1 = runif(250,  5000, 12000)
# Then add a column rmse computed by map2_dbl(b0, b1, rmse_fn, data = d100).
# Store in `search`.

```

## Task 4: Report the best pair

```{r task4}
# TODO: Use slice_min(rmse, n = 1) to find the row with lowest RMSE.
# Print b0, b1, and rmse.

```

## Task 5: Heatmap of the loss surface

```{r task5}
# TODO: Plot a scatter of b0 (x) vs b1 (y) from search, coloured by rmse.
# Use scale_colour_viridis_c() so low RMSE is dark and high is bright.
# Add axis labels and a title.

```
