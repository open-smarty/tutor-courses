---
title: "Exercise: GLMs — Logistic and Poisson Regression"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE, message = FALSE, warning = FALSE)
```

```{r load-packages}
library(tidyverse)
library(modelr)
library(nycflights13)
library(broom)
```

```{r prepare-data}
flights_clean <- flights |>
  filter(!is.na(dep_delay)) |>
  mutate(
    long_delay = as.integer(dep_delay > 30),
    hour       = as.numeric(hour),
    month      = as.numeric(month)
  )
```

## Task 1: Explore the binary outcome

```{r task1}
# TODO: Compute the overall proportion of flights with long_delay = 1.
# Also compute proportions by hour of day using group_by(hour) |> summarise().
# Plot: proportion of long delays vs hour as a bar or point plot.

```

## Task 2: Fit the null and logistic models

```{r task2}
# TODO: Fit:
#   mod_null  <- glm(long_delay ~ 1,              family = binomial(), data = flights_clean)
#   mod_logit <- glm(long_delay ~ hour + month,   family = binomial(), data = flights_clean)
# Print summary(mod_logit).

```

## Task 3: Odds ratios

```{r task3}
# TODO: Compute exp(coef(mod_logit)) to get odds ratios.
# Interpret the 'hour' coefficient: "Each additional hour of the day
# multiplies the odds of a long delay by ___."

```

## Task 4: Predicted probability curve

```{r task4}
# TODO: Create a data_grid(hour = 0:23, month = 7).
# Use add_predictions(mod_logit, type = "response") to get probabilities.
# Plot predicted probability vs hour. Add a horizontal line at 0.5.

```

## Task 5: AIC comparison

```{r task5}
# TODO: Compare AIC(mod_null, mod_logit).
# How much lower is mod_logit's AIC? What does this tell you about
# whether hour and month matter for predicting long delays?

```
