---
title: "Exercise: Robust Regression and GAMs"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE, message = FALSE, warning = FALSE)
```

```{r load-packages}
library(tidyverse)
library(modelr)
library(MASS)
library(mgcv)
library(broom)
```

```{r load-data}
data("sim1",    package = "modelr")
data("diamonds", package = "ggplot2")
```

## Task 1: OLS vs robust regression with outliers

```{r task1}
# TODO: Create sim1_out by adding 20 to y for rows 5 and 15.
# Fit:  mod_ols <- lm(y ~ x, data = sim1_out)
#       mod_rob <- MASS::rlm(y ~ x, method = "M", data = sim1_out)
# Create a comparison data frame with b0, b1 for each method.
# Plot sim1_out scatter + two fitted lines (OLS in gold, robust in blue).
# Note which line is pulled upward by the outliers.

```

## Task 2: Fit a GAM on diamonds

```{r task2}
# TODO: Fit:
# mod_gam <- gam(log(price) ~ s(log(carat)) + s(depth) + cut,
#               data = diamonds, method = "REML")
# Print summary(mod_gam). Read the EDF values for s(log(carat)) and s(depth).

```

## Task 3: Is s(depth) linear or non-linear?

```{r task3}
# TODO: From the summary output, look at the EDF for s(depth).
# EDF ≈ 1 → approximately linear; EDF >> 1 → non-linear.
# Write a one-line interpretation in a comment.

```

## Task 4: Visualise the smooths

```{r task4, fig.width=10, fig.height=4}
# TODO: Call plot(mod_gam, pages = 1, shade = TRUE).
# Describe what each smooth looks like in a comment.

```

## Task 5: AIC comparison — GAM vs linear model

```{r task5}
# TODO: Fit mod_lm <- lm(log(price) ~ log(carat) + depth + cut, data = diamonds).
# Compare AIC(mod_lm, mod_gam). Which wins and why?

```
