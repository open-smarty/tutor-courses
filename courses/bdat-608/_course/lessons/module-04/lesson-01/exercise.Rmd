---
title: "Exercise: Formula Notation, Model Matrix, and Categorical Predictors"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE, message = FALSE, warning = FALSE)
```

```{r load-packages}
library(tidyverse)
library(modelr)
library(broom)
```

## Task 1: Inspect the model matrix for cut

```{r task1}
data("diamonds", package = "ggplot2")

# TODO: Run model.matrix(~ cut, data = diamonds |> slice_head(n = 10)).
# Print the result.
# In comments: which level is the reference? What does each column mean?

```

## Task 2: Fit cut-only model

```{r task2}
# TODO: Fit lm(log(price) ~ cut, data = diamonds). Store as mod_cut.
# Print coef(mod_cut) and exp(coef(mod_cut)).
# Interpret the intercept and the cutIdeal coefficient in plain English.

```

## Task 3: Change the reference level

```{r task3}
# TODO: Create a copy of diamonds with cut releveled to "Ideal" as the reference:
#   diamonds2 <- diamonds |> mutate(cut = relevel(cut, ref = "Ideal"))
# Re-fit: mod_cut2 <- lm(log(price) ~ cut, data = diamonds2).
# Compare coef(mod_cut2) to coef(mod_cut).
# Does R² change? (Compare summary(mod_cut)$r.squared vs summary(mod_cut2)$r.squared)

```

## Task 4: Add log(carat) to control for size

```{r task4}
# TODO: Fit lm(log(price) ~ log(carat) + cut, data = diamonds). Store as mod_carat_cut.
# Print and exponentiate the coefficients.
# Compare the cut coefficients to those in mod_cut (Task 2).
# Explain in a comment: why do the signs change once we control for carat?

```
