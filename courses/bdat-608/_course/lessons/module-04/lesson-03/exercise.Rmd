---
title: "Exercise: Splines, Cross-Validation, and Missing Values"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE, message = FALSE, warning = FALSE)
```

```{r load-packages}
library(tidyverse)
library(modelr)
library(splines)
library(caret)
library(mice)
```

```{r load-data}
data("diamonds", package = "ggplot2")
data("sim5",    package = "modelr")
```

## Task 1: Natural spline fits on sim5

```{r task1}
# TODO: Fit lm(y ~ ns(x, df)) for df = 1, 3, 5 on sim5.
# Create a data_grid(x = seq_range(x, 100)) and add predictions for all three.
# Plot: scatter of (x, y) + geom_line for each df, coloured by df value.

```

## Task 2: 10-fold CV to choose df

```{r task2}
set.seed(42)
# TODO: For df = 1:8, run 10-fold CV:
#   folds <- createFolds(sim5$y, k = 10)
#   For each fold: train on remaining folds, predict on held-out fold, compute RMSE.
#   Average the 10 fold RMSEs for each df.
# Plot CV-RMSE vs df as a line plot. Report which df minimises CV-RMSE.

```

## Task 3: na.omit vs na.exclude

```{r task3}
# TODO: Create diam_miss by setting 10% of carat values to NA:
#   diam_miss <- diamonds |> mutate(carat = ifelse(runif(n()) < 0.10, NA, carat))
# Fit lm(log(price) ~ log(carat), data = diam_miss) — na.omit is the default.
# Print nobs(). Then re-fit with na.action = na.exclude. Print nobs().
# Run residuals() on each fit. Are the lengths different? How?

```

## Task 4: Multiple imputation with mice

```{r task4}
set.seed(1)
# TODO: Create diam_miss as above (seed = 1).
# Apply mice(diam_miss |> select(price, carat, cut), m = 5, method = "pmm").
# Fit with: fit <- with(imp, lm(log(price) ~ log(carat) + cut))
# Pool and summarise: pool(fit) |> summary()
# Compare the std.error for log(carat) to the complete-case analysis.

```
