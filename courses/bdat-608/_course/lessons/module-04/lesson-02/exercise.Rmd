---
title: "Exercise: Interactions and Transformations"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE, message = FALSE, warning = FALSE)
```

```{r load-packages}
library(tidyverse)
library(modelr)
library(broom)
```

```{r load-data}
data("diamonds", package = "ggplot2")
```

## Task 1: Fit the additive model

```{r task1}
# TODO: Fit lm(log(price) ~ log(carat) + cut, data = diamonds).
# Store as mod_add. Print summary().

```

## Task 2: Fit the interaction model

```{r task2}
# TODO: Fit lm(log(price) ~ log(carat) * cut, data = diamonds).
# Store as mod_int. Print summary().
# Note: log(carat) * cut expands to log(carat) + cut + log(carat):cut

```

## Task 3: AIC comparison

```{r task3}
# TODO: Compare AIC(mod_add, mod_int). Which model is preferred?

```

## Task 4: Prediction grid — are lines parallel?

```{r task4}
# TODO: Create a data_grid with:
#   carat = seq_range(carat, n = 10)
#   cut (all levels)
# Use gather_predictions(mod_add, mod_int) to get predictions from both models.
# Back-transform with exp() to price scale.
# Plot: x = carat, y = predicted price, colour = cut, facet by model.
# Are the lines parallel in the additive panel?

```

## Task 5: Interpret the interaction coefficient

```{r task5}
# TODO: Extract the coefficient for "log(carat):cutIdeal" from mod_int.
# Write a plain-English interpretation:
# "The slope of log(carat) is ___ units ___ for Ideal-cut compared to Fair-cut."

```

## Task 6: Polynomial model

```{r task6}
# TODO: Fit lm(log(price) ~ poly(log(carat), 2) + cut, data = diamonds).
# Store as mod_poly. Is the quadratic term (degree 2) significant (p < 0.05)?
# Compare AIC(mod_add, mod_poly).

```
