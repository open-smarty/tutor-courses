---
title: "BDAT 608 | Module 4, Lesson 2 — Interactions and Transformations"
author: "Your Name"
date: "`r Sys.Date()`"
output:
  html_document:
    toc: true
    theme: flatly
    highlight: tango
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE, message = FALSE, warning = FALSE,
                      fig.width = 9, fig.height = 5)
```

```{r libraries}
library(tidyverse)
library(modelr)
options(na.action = na.warn)
data("sim1", package = "modelr")
data("sim3", package = "modelr")
sim1_base <- sim1
```

---

## Task 1 — Fit Models on sim3

```{r fit-sim3}
# TODO: Fit mod_add (additive) and mod_int (interaction) on sim3.
# Use gather_predictions() to compare fitted lines.
# Plot: aes(x1, y, colour = x2) + geom_line(aes(y = pred)) + facet_wrap(~ model)
```

---

## Task 2 — Residual Comparison

```{r residuals-sim3}
# TODO: Use gather_residuals(mod_add, mod_int) to plot residuals.
# Use facet_grid(model ~ x2) and add geom_ref_line(h = 0).
# What pattern do you see in mod_add that is absent in mod_int?
```

---

## Task 3 — AIC Comparison

```{r aic-sim3}
# TODO: Compare AIC(mod_add, mod_int). Which is preferred?
# Print R² for both models.
```

---

## Task 4 — I() vs ^ in Formulas

```{r formula-demo}
# TODO: Show the model matrix for:
# (a) y ~ x^2 + x   (what does R actually produce?)
# (b) y ~ I(x^2) + x  (correct quadratic column)
# Comment on the difference.
```

---

## Reflection

<!-- If the interaction model has higher AIC than the additive model on some
     dataset, what does this tell us about the data? What principle does this
     illustrate about model selection? (2–3 sentences) -->
