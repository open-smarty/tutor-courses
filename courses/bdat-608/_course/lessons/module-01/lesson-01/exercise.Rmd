---
title: "Exercise: What Is a Statistical Model?"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE, message = FALSE, warning = FALSE)
```

```{r load-packages}
library(tidyverse)
library(modelr)
```

## Task 1: Explore the diamonds dataset

```{r task1}
# Load the diamonds dataset (it ships with ggplot2, which is part of tidyverse)
data("diamonds", package = "ggplot2")

# Inspect the data
glimpse(diamonds)
summary(diamonds)
```

## Task 2: Scatter plot of price vs carat coloured by cut

```{r task2}
# TODO: Create a scatter plot of price (y) vs carat (x), coloured by cut.
# Use geom_point() with alpha = 0.3 so overlapping points are visible.
# Add a title and minimal theme.

```

## Task 3: Fit a simple linear model

```{r task3}
# TODO: Fit a simple linear model: price ~ carat
# Store the result in a variable called mod1.
# Print the model summary with summary().

```

## Task 4: Add residuals and inspect the pattern

```{r task4}
# TODO: Use add_residuals(mod1) to attach a column called "resid" to diamonds.
# Store the augmented dataset as diamonds_resid.
# Then plot residuals on the y-axis vs carat on the x-axis.
# Add a horizontal reference line at zero.
# What pattern do you see? Write a one-sentence interpretation as a comment.

```
